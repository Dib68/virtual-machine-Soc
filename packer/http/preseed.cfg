# preseed.cfg - installazione automatica Kali/Debian per Packer
# Crea l'utente 'vagrant' (password 'vagrant') con privilegi sudo.

d-i debian-installer/locale string it_IT.UTF-8
d-i keyboard-configuration/xkb-keymap select it

d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string cybersec-ai
d-i netcfg/get_domain string localdomain

# Mirror
d-i mirror/country string manual
d-i mirror/http/hostname string http.kali.org
d-i mirror/http/directory string /kali
d-i mirror/http/proxy string

# Orologio
d-i clock-setup/utc boolean true
d-i time/zone string Europe/Rome
d-i clock-setup/ntp boolean true

# Utente root disabilitato, crea utente vagrant
d-i passwd/root-login boolean false
d-i passwd/user-fullname string Vagrant
d-i passwd/username string vagrant
d-i passwd/user-password password vagrant
d-i passwd/user-password-again password vagrant
d-i user-setup/allow-password-weak boolean true

# Partizionamento automatico su tutto il disco
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

# Pacchetti di base
tasksel tasksel/first multiselect standard, desktop, kali-desktop-xfce
d-i pkgsel/include string sudo openssh-server curl
popularity-contest popularity-contest/participate boolean false

# GRUB
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default

# Aggiungi vagrant a sudoers senza password (per il provisioning Packer)
d-i preseed/late_command string \
  echo 'vagrant ALL=(ALL) NOPASSWD: ALL' > /target/etc/sudoers.d/vagrant ; \
  in-target chmod 440 /etc/sudoers.d/vagrant

d-i finish-install/reboot_in_progress note
