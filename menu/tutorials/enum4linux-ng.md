# enum4linux-ng - enumerazione SMB/Windows
## Uso base
```
enum4linux-ng 10.0.0.5
enum4linux-ng -A 10.0.0.5         # tutto (utenti, gruppi, share, policy)
```
Versione moderna di enum4linux per la ricognizione di host Windows/Samba.
